import { FloorTypeColorPipe } from './floor-type-color.pipe';

describe('FloorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new FloorTypeColorPipe();
    expect(pipe).toBeTruthy();
  });
});
