import { FloorTypeStringPipe } from './floor-type-string.pipe';

describe('FloorTypeStringPipe', () => {
  it('create an instance', () => {
    const pipe = new FloorTypeStringPipe();
    expect(pipe).toBeTruthy();
  });
});
